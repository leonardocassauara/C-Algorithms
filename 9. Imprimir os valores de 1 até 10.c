# include <stdio.h>
# include <stdlib.h>

// Imprimir os números de 1 até 10 (while loop)

int main(){
	int contador=0;
	
	while (contador <= 9)
	{
		contador++;
		printf("%d\n", contador);
	}
	return 0;
}
